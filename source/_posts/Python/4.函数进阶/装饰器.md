---
title: Python3 装饰器
date: 2026-07-30 10:00:00
tags:
- Python
categories:
- Python
---

## 什么是装饰器

装饰器本质是一个**接收函数并返回新函数的高阶函数**，用于在不修改原函数代码的前提下增加功能（日志、鉴权、缓存、重试等）。
`@decorator` 只是语法糖：

```python
@log
def hello(): ...

# 完全等价于
def hello(): ...
hello = log(hello)
```

## 基本装饰器

```python
import functools

def log(func):
    @functools.wraps(func)          # 保留原函数的 __name__、__doc__ 等元信息
    def wrapper(*args, **kwargs):
        print(f"调用 {func.__name__}, 参数: {args} {kwargs}")
        result = func(*args, **kwargs)
        print(f"{func.__name__} 返回: {result}")
        return result
    return wrapper

@log
def add(a, b):
    return a + b

add(1, 2)
# 调用 add, 参数: (1, 2) {}
# add 返回: 3
```

> 不加 `functools.wraps` 时，`add.__name__` 会变成 `'wrapper'`。

## 带参数的装饰器（三层嵌套）

```python
import functools

def repeat(times):
    def decorator(func):
        @functools.wraps(func)
        def wrapper(*args, **kwargs):
            for _ in range(times):
                result = func(*args, **kwargs)
            return result
        return wrapper
    return decorator

@repeat(times=3)
def say():
    print("hi")

say()   # 打印 3 次 hi
```

## 类装饰器

```python
class Counter:
    def __init__(self, func):
        self.func = func
        self.count = 0

    def __call__(self, *args, **kwargs):
        self.count += 1
        print(f"第 {self.count} 次调用")
        return self.func(*args, **kwargs)

@Counter
def task():
    pass

task(); task()   # 第 1 次调用 / 第 2 次调用
```

## 多个装饰器的执行顺序

```python
@a
@b
def f(): ...
# 等价于 f = a(b(f))，装饰顺序自下而上，执行时自上而下
```

## 常见实战示例：计时装饰器

```python
import time, functools

def timeit(func):
    @functools.wraps(func)
    def wrapper(*args, **kwargs):
        start = time.perf_counter()
        result = func(*args, **kwargs)
        print(f"{func.__name__} 耗时 {time.perf_counter() - start:.4f}s")
        return result
    return wrapper
```

## 标准库中的常用装饰器

- `@functools.wraps`：保留被装饰函数元信息
- `@functools.lru_cache`：结果缓存
- `@property`、`@staticmethod`、`@classmethod`：类相关
- `@contextlib.contextmanager`：生成器转上下文管理器
