---
title: 组件通信与生命周期
date: 2026-07-30 10:00:00
tags:
- Vue3
- 组件通信
- 生命周期
categories:
- 前端
- 框架
- Vue3
---

# 组件通信与生命周期

Vue3 组件通信方式多样，按场景选择；生命周期在组合式 API 中以 `onXxx` 形式注册。

## 1. 组件通信方式

### Props / Emit（父子）

```vue
<!-- 父 -->
<Child :title="msg" @submit="onSubmit" />
<!-- 子 -->
<script setup lang="ts">
const props = defineProps<{ title: string }>()
const emit = defineEmits<{ (e: 'submit', v: string): void }>()
emit('submit', 'hello')
</script>
```

### v-model（双向绑定）

Vue3 支持多个 `v-model`：

```vue
<Child v-model:name="name" v-model:age="age" />
```

子组件：

```ts
const props = defineProps<{ name: string; age: number }>()
const emit = defineEmits<{
  (e: 'update:name', v: string): void
  (e: 'update:age', v: number): void
}>()
```

### provide / inject（跨层级）

```ts
// 祖先
import { provide, ref } from 'vue'
provide('key', ref('value'))

// 后代
import { inject } from 'vue'
const v = inject('key')
```

### 其他方式

- 事件总线：Vue3 移除 `$on`，推荐用 `mitt` 等第三方库。
- 全局状态：Pinia（见 [状态管理 Pinia](5.状态管理Pinia.md)）。
- 模板 ref 访问子组件实例：`const child = ref()` + `<Child ref="child" />`。

## 2. 生命周期

| Vue2 | Vue3（组合式） | 说明 |
| --- | --- | --- |
| `beforeCreate` | —（用 `setup`） | 组合式 `setup` 即替代 |
| `created` | —（用 `setup`） | 同上 |
| `beforeMount` | `onBeforeMount` | 挂载前 |
| `mounted` | `onMounted` | 挂载后（DOM 可用） |
| `beforeUpdate` | `onBeforeUpdate` | 更新前 |
| `updated` | `onUpdated` | 更新后 |
| `beforeUnmount` | `onBeforeUnmount` | 卸载前（Vue2 的 `beforeDestroy`） |
| `unmounted` | `onUnmounted` | 卸载后（Vue2 的 `destroyed`） |

```ts
import { onMounted, onUnmounted } from 'vue'
onMounted(() => { /* DOM 就绪 */ })
onUnmounted(() => { /* 清理定时器/事件监听 */ })
```

## 3. 插槽 slot

```vue
<!-- 子：具名 + 作用域插槽 -->
<slot name="header" :user="user" />
<!-- 父 -->
<template #header="{ user }">{{ user.name }}</template>
```

## 参考

- 响应式基础见 [响应式原理与 Composition API](2.响应式原理与Composition%20API.md)
