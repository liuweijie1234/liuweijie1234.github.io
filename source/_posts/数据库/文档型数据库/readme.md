# 2. 文档型数据库

以半结构化的文档（BSON/JSON）为存储单元，schema 灵活、字段可嵌套，写入模式宽松，水平扩展（分片）能力强。适合内容管理、物联网、用户画像、敏捷迭代的业务模型。

## 本分组包含

- **MongoDB**：文档型数据库的事实标准。通过副本集（Replica Set）实现高可用，通过分片（Sharding）实现海量水平扩展，支持聚合管道（Aggregation Pipeline）与多文档事务。

> 与关系型数据库的对比见 `MongoDB → 10.与关系型数据库对比`。
